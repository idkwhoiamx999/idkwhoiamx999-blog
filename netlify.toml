[build]
  command = "npm install && npm run build:css && hugo --config exampleSite/hugo.toml --contentDir exampleSite/content --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.161.1"
  NODE_VERSION = "20"
